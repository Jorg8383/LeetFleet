akka {
    actor {
        provider = "cluster"
        serialization-bindings {
           "service.message.MySerialisable" = jackson-json
        }
        serializers {
            jackson-json = "akka.serialization.jackson.JacksonJsonSerializer"
        }
    }
   remote {
    artery {
      transport = tcp # See Selecting a transport below
      canonical.hostname = "car"     # external (logical) hostname
      canonical.port = 2551             # external (logical) port

      # bind.hostname = "broker"   # internal (bind) hostname
      # bind.port = 2551            # internal (bind) port
    }
  }
}